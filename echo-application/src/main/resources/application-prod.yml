server:
  port: 8081

spring:
  config:
    activate:
      on-profile: prod


management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: "health,info,metrics,prometheus"
