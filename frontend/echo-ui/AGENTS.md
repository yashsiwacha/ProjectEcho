<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking c# Frontend Office AGENTS.md

This document defines the responsibilities and standards for the Frontend Engineering team within Project Echo.

## Purpose
- Implement UI components following the design system.
- Ensure accessibility (WCAG 2.1 AA).
- Maintain high performance (CWV metrics).
- Write comprehensive unit and integration tests.

## Standards
- Use vanilla CSS with modern design patterns (glassmorphism, gradients).
- Follow responsive design best practices.
- No hard‑coded secrets.
- All code must pass the CodeRabbit reviewer.
iffer from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
